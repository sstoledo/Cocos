import { LoginForm } from "@auth/form";

export const metadata = {
  title: 'Login Cococars',
  description: 'Página del login de cococars',
};

export default function LoginPage() {
  return (
    <LoginForm />
  );
}