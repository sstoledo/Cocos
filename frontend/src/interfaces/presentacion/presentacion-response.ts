export interface PresentacionResponse {
  id : string;
  name: string;
  createAt: Date;
  updateAt: Date;
  isActive: boolean;
}

export interface ComboPresentacion {
  id: string;
  name: string;
}
